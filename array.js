// Напиши функцію, яка приймає масив чисел і повертає новий масив, у якому всі числа помножені на 2.

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false;
//     }

//     let newArr = [];

//     for (let i = 0; i < arr.length; i++) {
//         newArr.push(arr[i] * 2)
//     }

//     return newArr;
// }

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Подумай, які методи або цикли можуть допомогти перебрати масив і перевірити, чи є число парним.

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false;
//     }

//     let sum = 0;

//     for (let i = 0; i < arr.length; i++) {
//         if (arr[i] % 2 === 0) {
//             sum += arr[i]
//         }
//     }

//     return sum;
// }

// console.log(func(3))

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Напиши функцию, которая возвращает массив всех чисел, которые встречаются в массиве больше одного раза.

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false;
//     }

//     let newArr = [];
//     let count = {};

//     for (let i = 0; i < arr.length; i++) {
//         count[arr[i]] = (count[arr[i]] || 0) + 1
//     }

//     for (const key in count) {
//         if (count[key] > 1) {
//             newArr.push(Number(key))
//         }
//     }

//     return newArr;
// }

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Порахувати суму всіх чисел у масиві.

// const arr = [10, 10, 10, 14];

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false
//     }

//     let sum = 0;

//     arr.forEach(element => {
//         sum += element;
//     });

//     return sum;
// }

// через reduce

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false;
//     }

//     return arr.reduce((sum, element) => sum + element, 0);
// }

// console.log(func(arr))

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Знайти максимальне та мінімальне значення у масиві.

// const arr = [10, 55, 1, 44];

// function func(arr) {
//     if (!Array.isArray(arr)) {
//         return false
//     }

//     let maxEl = arr[0];
//     let minEl = arr[0];

//     arr.forEach(element => {
//         if (element > maxEl) {
//             maxEl = element
//         } else if(element < minEl) {
//             minEl = element;
//         }
//     });

//     return [minEl, maxEl];
// }

// console.log(func(arr))

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Перевірити, чи всі елементи масиву унікальні.

// const arr = [10, 11, 55, 1, 1, 44, 11, 12];

// function func(arr) {
//     let obj = {};
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         obj[arr[i]] = (obj[arr[i]] || 0) + 1;
//     }

//     for (let key in obj) {
//         if (obj[key] === 1) {
//             result.push(Number(key))
//         }
//     }

//     return result;
// }

// console.log(func(arr))

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Знайти середнє арифметичне чисел у масиві
// 👉 Напиши функцію findAverage(arr), яка повертає середнє значення всіх чисел у масиві.

// function findAverage(arr) {
//     if (arr.length === 0) {
//         return [];
//     }

//     return arr.reduce((acc, num) => acc + num, 0) / arr.length
// }

// console.log(findAverage([10, 20, 30, 40, 50])); // 30
// console.log(findAverage([5, 15, 25])); // 15
// console.log(findAverage([])); // 0 (Якщо масив порожній, поверни 0)

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Перетворити масив слів у один рядок
// 👉 Напиши функцію joinWords(words), яка об'єднує всі слова у рядок через пробіл.

// function joinWords(words) {
//     return words.reduce((acc, word) => acc + (acc ? " " : "") + word, "")
// }

// console.log(joinWords(["Hello", "world", "!"])); // "Hello world !"
// console.log(joinWords(["JavaScript", "is", "awesome"])); // "JavaScript is awesome"
// console.log(joinWords([])); // "" (Порожній рядок, якщо масив порожній)

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Перевернути масив
// Напиши функцію, яка перевертатиме масив без використання методу reverse(). Тобто з першого елемента має стати останнім, з другого — передостаннім і т.д.

// function reverseArray(arr) {
//     if (arr.length === 0) {
//         return []
//     }

//     let result = [];

//     for (let i = arr.length - 1; i >= 0; i--) {
//         result.push(arr[i]);
//     }

//     return result;
// }

// console.log(reverseArray([1, 2, 3, 4]))  // має повернути [4, 3, 2, 1]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Знайти максимальне і мінімальне значення
// Напиши функцію, яка знаходить максимальне та мінімальне значення в масиві. Не використовуйте Math.max, Math.min або методи масивів.

// function findMinMax(arr) {
//     let min = Infinity;
//     let max = -Infinity;

//     for (let i = 0; i < arr.length; i++) {
//         if (arr[i] < min) {
//             min = arr[i]
//         }
//         if (arr[i] > max) {
//             max = arr[i]
//         }
//     }

//     return {min, max};
// }

// console.log(findMinMax([5, 3, 99, 1, 9, 1880, 7]))  // має повернути {min: 1, max: 9}

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Вивести всі парні числа в масиві
// Напиши функцію, яка перебирає масив і виводить лише парні числа без використання методу filter.

// function findEvenNumbers(arr) {
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         if (arr[i] % 2 === 0) {
//             result.push(arr[i])
//         }
//     }

//     return result;
// }

// console.log(findEvenNumbers([1, 2, 3, 4, 5, 6])) // має повернути [2, 4, 6]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Підсумок всіх чисел
// Напиши функцію, яка знаходить суму всіх чисел в масиві, обходячи його вручну.

// function sumArray(arr) {
//     return arr.reduce((acc, num) => acc + num, 0)
// }

// console.log(sumArray([1, 2, 3, 4])) // має повернути 10

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Перевірити, чи всі елементи масиву однакові
// Напиши функцію, яка перевіряє, чи всі елементи в масиві однакові (без використання every або forEach).

// function checkEqual(arr) {
//     for (let i = 0; i < arr.length - 1; i++) {
//         if (arr[i] !== arr[i + 1]) {
//             return false
//         }
//     }

//     return true
// }

// console.log(checkEqual([5, 5, 5, 5]))  // має повернути true
// console.log(checkEqual([5, 5, 6, 5]))  // має повернути false

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Видалити всі повторювані елементи в масиві
// Напиши функцію, яка видаляє всі дублікати з масиву без використання методів filter чи Set.

// function removeDuplicates(arr) {
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         if(result.indexOf(arr[i]) === -1) {
//             result.push(arr[i])
//         }
//     }

//     return result;
// }

// console.log(removeDuplicates([1, 2, 2, 3, 3, 4]))  // має повернути [1, 2, 3, 4]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Знайти індекс елемента
// Напиши функцію, яка знаходить індекс першого входження елемента в масив, не використовуючи метод indexOf.

// function findIndex(arr, index) {
//     let res;

//     for (let i = 0; i < arr.length; i++) {
//         if(arr[i] === index) {
//             res = i
//         }
//     }

//     return res;
// }

// console.log(findIndex([1, 2, 3, 4], 3))  // має повернути 2

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Вивести елементи, що зустрічаються більше одного разу
// Напиши функцію, яка виводить елементи масиву, які зустрічаються більше одного разу. Не використовуйте filter чи інші методи.

// function findDuplicates(arr) {
//     let count = {};
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         count[arr[i]] = (count[arr[i]] || 0) + 1;
//     }

//     for (let key in count) {
//         if (count[key] > 1) {
//             result.push(Number(key))
//         }
//     }

//     return result;
// }

// console.log(findDuplicates([1, 2, 3, 1, 4, 5, 2]))  // має повернути [1, 2]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Опис: Напиши функцію, яка переставляє елементи масиву у зворотному порядку без створення нового масиву.
// Обмеження: Не можна використовувати reverse() та додаткові структури даних.

// function reverseInPlace(arr) {
//   let left = 0;
//   let right = arr.length - 1;

//   while (left < right) {
//     let temp = arr[left];
//     arr[left] = arr[right];
//     arr[right] = temp;
//     left++;
//     right--;
//   }

//   return arr;
// }

// let arr = [1, 2, 3, 4, 5];

// console.log(reverseInPlace(arr)); // [5, 4, 3, 2, 1]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Зсув масиву (циклічний)
// Опис: Напиши функцію, яка зміщує елементи масиву вправо на k позицій.
// Обмеження: Не можна створювати додатковий масив, зміщення має відбуватися на місці.

// function rotateArray(arr, k) {
//     if (k > arr.length) {
//       k = k % arr.length; // Чтобы не делать лишних вращений
//     }

//     function reversePart(arr, start, end) {
//       while (start < end) {
//         let temp = arr[start];
//         arr[start] = arr[end];
//         arr[end] = temp;
//         start++;
//         end--;
//       }
//     }

//     arr.reverse(); // 1. Переворачиваем весь массив
//     reversePart(arr, 0, k - 1); // 2. Переворачиваем первые k элементов
//     reversePart(arr, k, arr.length - 1); // 3. Переворачиваем оставшуюся часть

//     return arr;
//   }

// let nums = [1, 2, 3, 4, 5];
// console.log(rotateArray(nums, 2)); // [4, 5, 1, 2, 3]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

//  Найдовший зростаючий підмасив
// Опис: Напиши функцію, яка знаходить найдовший підмасив, де елементи йдуть у порядку зростання.

// function longestIncreasingSubarray(arr) {
//     // Твій код тут
// }

// console.log(longestIncreasingSubarray([1, 2, 2, 3, 4, 1, 7, 8]));
// [1, 7, 8] або [2, 3, 4] (один із варіантів)

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

//  Підрахунок унікальних елементів
// Опис: Напиши функцію, яка повертає кількість унікальних елементів у відсортованому масиві без додаткової пам'яті.
// Обмеження: Не можна використовувати Set.

// function countUniqueSorted(arr) {
//     // Твій код тут
// }

// console.log(countUniqueSorted([1, 1, 2, 2, 3, 3, 3, 4, 5]));  // 5

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

//  Знайти відсутній елемент у послідовності
// Опис: Дан масив довжиною n-1, що містить числа від 1 до n, але пропущено одне число. Знайди відсутнє число.
// Обмеження: O(n) за часом і O(1) за пам’яттю.

// function findMissingNumber(arr, n) {
//     // Твій код тут
// }

// console.log(findMissingNumber([1, 2, 4, 5, 6], 6));  // 3

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Перемістити всі нулі в кінець
// Опис: Напиши функцію, яка переміщує всі 0 у масиві в його кінець, зберігаючи порядок інших чисел.
// Обмеження: Не можна створювати новий масив.

// function moveZeros(arr) {
//     let nonZeroIndex = 0;

//     for (let i = 0; i < arr.length; i++) {
//       if (arr[i] !== 0) {
//         if (i !== nonZeroIndex) {
//           let temp = arr[nonZeroIndex];
//           arr[nonZeroIndex] = arr[i];
//           arr[i] = temp;
//         }
//         nonZeroIndex++;
//       }
//     }

//     return arr;
//   }

// let nums = [0, 1, 0, 3, 12];
// console.log(moveZeros(nums)); // [1, 3, 12, 0, 0]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Умова: Напиши функцію customSlice(arr, start, end), яка працює як Array.prototype.slice()

// function mySlice(arr, start, end) {
//   let result = [];

//   // Підтримка від'ємних індексів
//   if (start < 0) start = arr.length + start;
//   if (end < 0) end = arr.length + end;

//   // Гарантуємо, що start і end в межах
//   start = Math.max(0, start);
//   end = Math.min(arr.length, end);

//   arr.forEach((element, index) => {
//     if (index >= start && index < end) {
//       result.push(element);
//     }
//   });

//   return result;
// }

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Створи функцію mySplice(arr, start, deleteCount, ...items), яка:

// видаляє deleteCount елементів починаючи з індексу start,

// додає нові елементи (...items) у те саме місце,

// повертає масив видалених елементів,

// змінює вихідний масив arr.

// function mySplice(arr, start, deleteCount, ...items) {

// }

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// const array = [1, 2, 3];

// function myForEach(array, callback) {
//   for (let i = 0; i < array.length; i++) {
//     callback(array[i], i, array);
//   }
// }

// console.log(myForEach());

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Напиши функцию myMap(arr, callback), которая будет работать аналогично методу map.

// Условия:
// Она должна перебирать все элементы массива arr.

// Для каждого элемента вызывать переданную функцию callback с тремя аргументами: значение элемента, индекс и сам массив.

// Функция должна возвращать новый массив, в котором будут результат вызова callback для каждого элемента.

// function myMap(arr, callback) {
//   let result = [];

//   for (let i = 0; i < arr.length; i++) {
//     result.push(callback(arr[i], i, arr));
//   }

//   return result;
// }

// const numbers = [1, 2, 3, 4];

// const squared = myMap(numbers, (num) => num * num);
// console.log(squared);

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Метод должен:

// Пройти по всем элементам массива.

// Вызвать callback для каждого элемента.

// Если callback вернул true, элемент должен быть добавлен в новый массив.

// Вернуть новый массив.

// function myFilter(arr, callback) {
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         if (callback(arr[i], i, arr)) {
//             result.push(arr[i])
//         }
//     }

//     return result;
// }

// const numbers = [1, 2, 3, 4, 5];

// const evenNumbers = myFilter(numbers, (num) => num % 2 === 0);
// console.log(evenNumbers); // [2, 4]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Задача для реализации reduce:

// Метод reduce принимает два аргумента: функцию-колбэк и начальное значение аккумулятора. Функция-колбэк принимает четыре параметра: аккумулятор, текущий элемент, индекс элемента и сам массив.

// В результате работы reduce ты должен получить одно итоговое значение, которое получится путем последовательных вычислений.

// function myReduce(arr, callback, initialValue) {
//   let accamulator = initialValue;

//   for (let i = 0; i < arr.length; i++) {
//     accamulator = callback(accamulator, arr[i], i, arr);
//   }

//   return accamulator;
// }

// const sum = myReduce([1, 2, 3, 4], (acc, curr) => acc + curr, 0);
// console.log(sum); // 10

// const product = myReduce([1, 2, 3, 4], (acc, curr) => acc * curr, 1);
// console.log(product); // 24

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Метод find возвращает первый элемент массива, который удовлетворяет условию в переданной функции. Если ни один элемент не подходит — возвращает undefined.

// function myFind(arr, callback) {
//   for (let i = 0; i < arr.length; i++) {
//     if (callback(arr[i], i, arr)) {
//         return arr[i]
//     }
//   }

//   return undefined;
// }

// const arr = [5, 12, 8, 130, 44];

// console.log(myFind(arr, (el) => el > 10));      // 12
// console.log(myFind(arr, (el) => el > 200));     // undefined
// console.log(myFind(arr, (el) => el % 2 === 0)); // 12

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Реалізуй метод myFindIndex(arr, callback), який:

// проходить по масиву,

// викликає callback(element, index, array) для кожного елемента,

// повертає індекс першого елемента, для якого callback повертає true,

// якщо жоден елемент не підходить — повертає -1.

// function myFindIndex(arr, callback) {
//     for (let i = 0; i < arr.length; i++) {
//         if (callback(arr[i], i, arr)) {
//             return i;
//         }
//     }

//     return -1;
// }

// const arr = [5, 12, 8, 130, 44];

// console.log(myFindIndex(arr, el => el > 10)); // 1
// console.log(myFindIndex(arr, el => el > 200)); // -1
// console.log(myFindIndex(arr, (el, i) => i === 2)); // 2

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Цей метод перевіряє, чи всі елементи масиву задовольняють умову.
// Повертає true, якщо всі пройшли перевірку, і false — якщо хоч один не пройшов.

// function myEvery(arr, callback) {
//     for (let i = 0; i < arr.length; i++) {
//         if (!callback(arr[i], i, arr)) {
//             return false;
//         }
//     }

//     return true;
// }

// console.log(myEvery([1, 2, 3, 4], x => x > 0)); // true
// console.log(myEvery([1, 2, -3, 4], x => x > 0)); // false
// console.log(myEvery(['a', 'aa', 'aaa'], str => str.length >= 1)); // true
// console.log(myEvery([2, 4, 6], n => n % 2 === 0)); // true
// console.log(myEvery([2, 4, 5], n => n % 2 === 0)); // false

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Реалізуй функцію mySome(arr, callback), яка:

// Повертає true, якщо хоча б один елемент масиву задовольняє умову з колбеку.
// Повертає false, якщо жоден елемент не задовольняє умову.

// function mySome(arr, callback) {
//     for (let i = 0; i < arr.length; i++) {
//         if(callback(arr[i], i, arr)) {
//             return true
//         }
//     }

//     return false;
// }

// console.log(mySome([1, 3, 5], x => x % 2 === 0)); // false
// console.log(mySome([1, 4, 5], x => x % 2 === 0)); // true
// console.log(mySome([], x => x > 0)); // false

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Метод flat() в JavaScript используется для "выравнивания" вложенных массивов на указанную глубину.
// Если у нас есть массив с вложенными массивами, flat() преобразует его в одномерный массив.

// function myFlat(arr, depth = 1) {
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         const current = arr[i];

//         if(Array.isArray(current) && depth > 0) {
//             result = result.concat(myFlat(current, depth - 1))
//         } else {
//             result.push(current)
//         }
//     }

//     return result;
// }

// const arr = [1, 2, [3, 4, [5, 6]]];
// console.log(arr.flat()); // [1, 2, 3, 4, [5, 6]]
// console.log(arr.flat(2)); // [1, 2, 3, 4, 5, 6]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Отлично, давай двигаться дальше! Одна из интересных задач — это реализация метода reduceRight().
// Этот метод работает аналогично reduce(), но выполняет обработку элементов с конца массива.

// function myReduceRight(arr, callback, initialValue) {
//     let acc;
//     let startIndex;

//     if (initialValue === undefined) {
//         acc = arr[arr.length - 1]
//         startIndex = arr.length - 2;
//     } else {
//         initialValue = arr[arr.length - 1]
//     }

//     for (let i = startIndex; i >= 0; i--) {
//         acc = callback(arr[i], i, arr)
//     }

//     return acc;
// }

// const arr = [1, 2, 3, 4];

// const result = myReduceRight(arr, (acc, currentValue) => {
//     return acc + currentValue;
// }, 0);

// console.log(result); // 10 (4 + 3 + 2 + 1)

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Твоя задача — реалізувати myFill

// Умови:

// Функція повинна змінювати оригінальний масив.

// Має приймати value, start, end.

// Повертає той самий масив, модифікований.

// function myFill(arr, value, start = 0, end = arr.length) {
//     for (let i = start; i < end; i++) {
//         arr[i] = value;
//     }

//     return arr;
// }

// const a = [1, 2, 3, 4, 5];
// console.log(myFill(a, 0, 1, 4)); // [1, 0, 0, 0, 5]

// const b = [1, 2, 3];
// console.log(myFill(b, 7)); // [7, 7, 7]

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Метод includes() перевіряє, чи міститься значення у масиві, і повертає true або false.

// function myIncludes(arr, searchElement, fromIndex = 0) {
//     let minusIndex;

//     if (fromIndex < 0) {
//         minusIndex = arr.length + fromIndex;
//         if (fromIndex < 0) fromIndex = 0;
//     }

//     for (let i = fromIndex; i < arr.length; i++) {
//         if (arr[i] === searchElement) {
//             return true
//         }
//     }

//     return false;
// }

// console.log(myIncludes([1, 2, 3], 2));         // true
// console.log(myIncludes([1, 2, 3], 4));         // false
// console.log(myIncludes([1, 2, 3], 3, 3));      // false
// console.log(myIncludes([1, 2, 3], 3, -1));     // true
// console.log(myIncludes(['a', 'b', 'c'], 'a')); // true

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Напиши функцію groupBy, яка приймає масив об'єктів і функцію-ключ (callback). Вона повинна повертати об’єкт, у якому елементи згруповані за результатом виклику callback.

// function groupBy(arr, callback) {
//     let obj = {};

//     for (let i = 0; i < arr.length; i++) {
//         const key = callback(arr[i]);

//         if (!obj[key]) {
//             obj[key] = [];
//         }

//         obj[key].push(arr[i])
//     }

//     return obj
// }

// const data = [
//   { name: "Alice", age: 21 },
//   { name: "Bob", age: 25 },
//   { name: "Charlie", age: 21 },
//   { name: "David", age: 25 },
//   { name: "Eve", age: 30 },
// ];

// const result = groupBy(data, (person) => person.age);

// console.log(result)
/*
  Очікуваний результат:
  {
    21: [{ name: 'Alice', age: 21 }, { name: 'Charlie', age: 21 }],
    25: [{ name: 'Bob', age: 25 }, { name: 'David', age: 25 }],
    30: [{ name: 'Eve', age: 30 }]
  }
  */

//   ________________________________________________________________________________________________________________________________________________________________________________________________________________

//   Створи функцію countBy(arr, callback), яка:

// перебирає масив,

// групує елементи за ключем, що повертає callback,

// рахує кількість елементів у кожній групі,

// повертає об'єкт з кількістю для кожної групи.

// function countBy(arr, callback) {
//   let grouped = {};

//   for (let i = 0; i < arr.length; i++) {
//     const key = callback(arr[i]);

//     if (!grouped[key]) {
//       grouped[key] = 1;
//     } else {
//       grouped[key]++;
//     }
//   }

//   return grouped;
// }

// const data = [6.1, 4.2, 6.3, 5.5, 5.1, 4.8];

// const result = countBy(data, Math.floor);

// console.log(result);

// ________________________________________________________________________________________________________________________________________________________________________________________________________________

// Реализуй функцию, которая будет превращать вложенные массивы в один плоский массив.

// function myFlatten(arr, depth = 1) {
//     let result = [];

//     for (let i = 0; i < arr.length; i++) {
//         const current = arr[i]
//         if (Array.isArray(current) && depth > 0) {
//             result.push(...myFlatten(current, depth - 1))
//         } else {
//             result.push(current)
//         }
//     }

//     return result;
// }

// const arr = [1, [2, 3], [4, [5, 6]], 7];
// console.log(myFlatten(arr));
